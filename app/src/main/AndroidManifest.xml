<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.capping.xinran.cappingnews" >

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"></uses-permission>
    <application
        android:name=".global.EngloryApplication"
        android:allowBackup="true"
        android:icon="${app_icon}"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".customs.test.TestCustomTextSwitcher"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.capping.xinran.cappingnews.base.webview.WebActivity"></activity>
    </application>
    <meta-data android:name="com.capping.xinran.cappingnews.global.imageloader.QGlideModule"
        android:value="GlideModule"/>
    <meta-data
        android:name="UMENG_CHANNEL"
        android:value="${UMENG_CHANNEL_ID}"/>

</manifest>
